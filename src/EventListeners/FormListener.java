package EventListeners;

/**
 * Interface that implements the handleFormSubmit method for the EventListener class.
 */
public interface FormListener extends EventListener {

	void handleFormSubmit(String action);
	
}
