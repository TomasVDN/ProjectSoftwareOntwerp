package EventListeners;

/**
 * Interface that implements the barMoved method for the EventListener class.
 */
public interface SeparatorBarMoveListener extends EventListener {

	/**
	 * Notify the listener that the bar is moved
	 */
	void seperatorBarMoved();
}
